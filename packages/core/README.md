# @moldig/core

The engine behind [moldig](https://www.npmjs.com/package/moldig): the index of what AI coding harnesses leave on disk, the adapters that read each harness, the detectors and the dependency graph. No terminal dependencies, so apps can embed it.

The API is not stable before 1.0. Requires Node.js >= 22.18.

MIT © Guillermo López
